[phases.install]
cmds = ["npm install -g pnpm@12.3.4", "pnpm install --no-frozen-lockfile --prod=false"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server build"]

[start]
cmd = "pnpm --filter @workspace/api-server start"